const db = require('./config/db'); db.query('ALTER TABLE medical_reports ADD COLUMN IF NOT EXISTS appointment_id UUID REFERENCES appointments(id) ON DELETE CASCADE, ADD COLUMN IF NOT EXISTS symptoms TEXT, ADD COLUMN IF NOT EXISTS treatment_plan TEXT, ADD COLUMN IF NOT EXISTS file_data BYTEA, ADD COLUMN IF NOT EXISTS file_mimetype VARCHAR(100), ADD COLUMN IF NOT EXISTS file_name VARCHAR(255);').then(()=>console.log('Fixed schema')).catch(console.error).finally(()=>process.exit())
